<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Sign In - Luxe Time</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <style>
        /* Custom colors based on your theme images */
        .bg-dark-black {
            background-color: #0d1310;
        }

        /* Main body background */
        .bg-card-dark {
            background-color: #1a322c;
        }

        /* Card background */
        .bg-primary {
            background-color: #38a169;
        }

        /* Green primary button */
        .text-primary {
            color: #38a169;
        }

        /* Green text */
        .ring-input {
            border-color: #38a169;
        }

        /* Focus ring for inputs */

        /* Input field specific style matching user modals */
        .custom-input {
            background-color: #10211a;
            /* Darker green for input background */
            border-width: 0;
            padding-top: 1rem;
            /* pt-4 */
            padding-bottom: 0.5rem;
            /* pb-2 */
            padding-left: 1rem;
            /* px-4 */
            font-size: 0.875rem;
            /* text-sm */
            box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
            transition: all 0.2s;
            border-radius: 0.5rem;
            /* rounded-lg */
        }

        .custom-input:focus {
            outline: none;
            box-shadow: 0 0 0 1px #38a169;
            /* focus:ring-primary */
        }

        <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>Dashboard - LUXE TIME Admin</title><script src="https://cdn.tailwindcss.com"></script><link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" /><link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">

        /* Custom colors based on your theme images */
        .bg-dark-black {
            background-color: #0d1310;
        }

        .bg-card-dark {
            background-color: #1a322c;
        }

        .bg-sidebar-dark {
            background-color: #12211d;
        }

        .bg-primary {
            background-color: #38a169;
        }

        .text-primary {
            color: #38a169;
        }

        .text-light-green {
            color: #68d391;
        }
    </style>
</head>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>


<body class="bg-black min-h-screen text-white flex font-sans"></body>



<%- body %>

    </body>

</html>