
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap');
        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: #050605;
            color: #ffffff;
            font-size: 14px;
        }
        .glass-panel {
            background: #181e18;
            border: 1px solid rgba(255, 255, 255, 0.03);
        }
        .success-glow {
            background: radial-gradient(circle at center, rgba(34, 197, 94, 0.08) 0%, transparent 70%);
        }
        .step-active { color: #22c55e; }
        .step-line-active { background: #22c55e; }
    </style>
</head>

    <main class="max-w-[1000px] mx-auto px-6 py-4">
        
        <div class="flex items-center justify-center gap-2 mb-2 mt-4">
            <div class="flex items-center gap-2 step-active">
                <i class="fa-solid fa-circle-check text-[10px]"></i>
                <span class="text-[10px] font-black uppercase tracking-widest">Cart</span>
            </div>
            <div class="w-10 h-[1px] step-line-active"></div>
            <div class="flex items-center gap-2 step-active">
                <i class="fa-solid fa-circle-check text-[10px]"></i>
                <span class="text-[10px] font-black uppercase tracking-widest">Checkout</span>
            </div>
            <div class="w-10 h-[1px] step-line-active"></div>
            <div class="flex items-center gap-2 step-active">
                <span class="w-5 h-5 rounded-full bg-[#22c55e] text-black flex items-center justify-center text-[10px] font-bold shadow-lg shadow-[#22c55e]/20">3</span>
                <span class="text-[10px] font-black uppercase tracking-widest">Confirmation</span>
            </div>
        </div>

        <div class="glass-panel rounded-3xl p-4 text-center mb-4 success-glow relative overflow-hidden">
            <div class="relative z-10">
                <div class="w-20 h-20 bg-[#22c55e] rounded-full flex items-center justify-center mx-auto mb-6 shadow-2xl shadow-[#22c55e]/40">
                    <i class="fa-solid fa-check text-3xl text-black"></i>
                </div>
                <h1 class="text-4xl font-black tracking-tighter uppercase mb-3">Order Confirmed!</h1>
                <p class="text-zinc-400 max-w-sm mx-auto mb-8 text-sm leading-relaxed">
                    Thank you for your purchase. Your premium timepiece is now being prepared for shipment.
                </p>
                <div class="inline-flex items-center bg-black/50 border border-white/10 px-6 py-3 rounded-2xl">
                    <span class="text-[10px] font-black text-zinc-500 uppercase tracking-widest mr-3">Order Reference:</span>
                    <span class="text-[11px] font-black text-[#22c55e]">#<%= order._id %></span>
                </div>
            </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-12 gap-4">
            <div class="lg:col-span-8 space-y-4">
                
                <div class="glass-panel rounded-2xl p-2">
                    <h3 class="text-[11px] font-black uppercase tracking-widest mb-6 flex items-center gap-2">
                        <i class="fa-solid fa-box-open text-[#22c55e]"></i> Ordered Items
                    </h3>
                    <div class="space-y-4">
                        <% order.items.forEach(item => { %>
                            <div class="flex items-center justify-between group border-b border-white/5 pb-4 last:border-0">
                                <div class="flex gap-4">
                                    <div class="w-16 h-16 bg-black rounded-xl border border-white/5 p-1">
                                        <img src="<%= item.variant.images[0].url %>" class="w-full h-full object-cover rounded-lg" alt="<%= item.product.name %>">
                                    </div>
                                    <div class="flex flex-col justify-center">
                                        <h4 class="font-bold text-sm group-hover:text-[#22c55e] transition"><%= item.product.name %></h4>
                                        <p class="text-[10px] text-zinc-500 font-bold uppercase mt-1">
                                            Qty: <%= item.quantity %> | Price: ₹<%= item.price.toLocaleString('en-IN') %>
                                        </p>
                                    </div>
                                </div>
                                <span class="font-bold text-sm">₹<%= (item.price * item.quantity).toLocaleString('en-IN') %></span>
                            </div>
                        <% }) %>
                    </div>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div class="glass-panel rounded-2xl p-6">
                        <h3 class="text-[10px] font-black uppercase text-zinc-500 mb-4 tracking-widest">Delivery Address</h3>
                        <p class="font-bold text-sm mb-1"><%= order.shippingAddress.fullName %></p>
                        <p class="text-xs text-zinc-400 leading-relaxed mb-3">
                            <%= order.shippingAddress.streetAddress %><br>
                            <%= order.shippingAddress.city %>, <%= order.shippingAddress.state %> - <%= order.shippingAddress.pincode %>
                        </p>
                        <p class="text-[10px] text-[#22c55e] font-bold">Mobile: +91 <%= order.shippingAddress.phone %></p>
                    </div>

                    <div class="glass-panel rounded-2xl p-6">
                        <h3 class="text-[10px] font-black uppercase text-zinc-500 mb-4 tracking-widest">Logistics Info</h3>
                        <div class="space-y-4">
                            <div class="flex items-center gap-3">
                                <div class="w-8 h-8 rounded-lg bg-zinc-900 flex items-center justify-center">
                                    <i class="fa-solid fa-credit-card text-[#22c55e] text-[10px]"></i>
                                </div>
                                <div>
                                    <p class="text-[9px] font-black text-zinc-500 uppercase">Payment</p>
                                    <p class="text-xs font-bold"><%= order.paymentMethod %></p>
                                </div>
                            </div>
                            <div class="flex items-center gap-3">
                                <div class="w-8 h-8 rounded-lg bg-zinc-900 flex items-center justify-center">
                                    <i class="fa-solid fa-truck-fast text-zinc-500 text-[10px]"></i>
                                </div>
                                <div>
                                    <p class="text-[9px] font-black text-zinc-500 uppercase">Status</p>
                                    <p class="text-xs font-bold uppercase text-[#22c55e]"><%= order.paymentStatus %></p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="lg:col-span-4">
                <div class="glass-panel rounded-2xl p-6 sticky top-6">
                    <h3 class="text-[11px] font-black uppercase tracking-widest mb-6">Payment Summary</h3>
                    
                    <div class="space-y-3 text-xs font-bold border-b border-white/5 pb-6 mb-6">
                        <div class="flex justify-between text-zinc-500">
                            <span>Subtotal</span>
                            <span class="text-white">₹<%= order.subtotal.toLocaleString('en-IN') %></span>
                        </div>
                        <div class="flex justify-between text-zinc-500">
                            <span>Discount</span>
                            <span class="text-red-500">-₹<%= order.discount.toLocaleString('en-IN') %></span>
                        </div>
                        <div class="flex justify-between text-zinc-500">
                            <span>Tax (GST 18%)</span>
                            <span class="text-white">₹<%= order.tax.toLocaleString('en-IN') %></span>
                        </div>
                    </div>

                    <div class="flex justify-between items-center mb-8">
                        <div>
                            <p class="text-[9px] font-black text-zinc-500 uppercase tracking-tighter">Total Amount Paid</p>
                            <p class="text-2xl font-black text-[#22c55e]">₹<%= order.totalAmount.toLocaleString('en-IN') %></p>
                        </div>
                        <div class="bg-[#22c55e]/10 text-[#22c55e] p-2 rounded-lg">
                            <i class="fa-solid fa-receipt"></i>
                        </div>
                    </div>

                    <div class="space-y-3">
                        <a href="/orders" class="w-full bg-[#22c55e] text-black py-4 rounded-xl text-[11px] font-black uppercase text-center tracking-widest hover:brightness-110 transition-all shadow-lg shadow-[#22c55e]/20 flex items-center justify-center gap-2">
                            View My Orders <i class="fa-solid fa-list"></i>
                        </a>
                        <a href="/shop" class="w-full bg-white/5 border border-white/10 text-white py-4 rounded-xl text-[11px] font-black uppercase text-center tracking-widest hover:bg-white/10 transition-all flex items-center justify-center gap-2">
                            <i class="fa-solid fa-arrow-left text-[9px]"></i> Continue Shopping
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </main>
