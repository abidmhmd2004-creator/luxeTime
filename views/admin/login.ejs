<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Sign In - Luxe Time</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <style>
        /* Custom colors based on your theme images */
        .bg-dark-black { background-color: #0d1310; } /* Main body background */
        .bg-card-dark { background-color: #1a322c; } /* Card background */
        .bg-primary { background-color: #38a169; } /* Green primary button */
        .text-primary { color: #38a169; } /* Green text */
        .ring-input { border-color: #38a169; } /* Focus ring for inputs */

        /* Input field specific style matching user modals */
        .custom-input {
            background-color: #10211a; /* Darker green for input background */
            border-width: 0;
            padding-top: 1rem; /* pt-4 */
            padding-bottom: 0.5rem; /* pb-2 */
            padding-left: 1rem; /* px-4 */
            font-size: 0.875rem; /* text-sm */
            box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
            transition: all 0.2s;
            border-radius: 0.5rem; /* rounded-lg */
        }
        .custom-input:focus {
            outline: none;
            box-shadow: 0 0 0 1px #38a169; /* focus:ring-primary */
        }
    </style>
</head>

<body class="bg-dark-black min-h-screen flex flex-col justify-center items-center font-sans">

    <div class="mb-8 flex flex-col items-center">
        <div class="text-3xl font-extrabold text-white flex items-center space-x-2">
            <img src="/path/to/luxe_time_logo.png" alt="Luxe Time Logo" class="w-8 h-8"/> 
            <span>LUXE TIME</span>
        </div>
        <p class="text-sm tracking-widest uppercase text-primary mt-2">ADMIN PANEL</p>
    </div>

    <div class="bg-card-dark rounded-xl shadow-2xl w-full max-w-sm mx-4 p-8 space-y-6">

        <div class="text-center space-y-1">
            <h2 class="text-2xl font-bold text-white">Admin Sign In</h2>
            <p class="text-sm text-neutral-400">Access the administration dashboard</p>
        </div>

        <form action="/admin/login" method="POST" class="space-y-5">
            
            <div class="relative">
                <label for="adminEmail" class="absolute left-4 top-1 text-[8px] uppercase text-neutral-500 font-medium tracking-wider">Email Address</label>
                <input 
                    type="email" 
                    id="adminEmail" 
                    name="email" 
                    placeholder="admin@luxetime.com" 
                    required 
                    class="custom-input w-full text-white" 
                    title="Please enter a valid email address."
                />
            </div>

            <div class="relative">
                <label for="adminPassword" class="absolute left-4 top-1 text-[8px] uppercase text-neutral-500 font-medium tracking-wider">Password</label>
                <input 
                    type="password" 
                    id="adminPassword" 
                    name="password" 
                    placeholder="••••••••" 
                    required 
                    minlength="8" 
                    class="custom-input w-full text-white" 
                    title="Password must be at least 8 characters long."
                />
            </div>

            <button 
                type="submit" 
                class="w-full flex items-center justify-center space-x-2 px-4 py-3 rounded-lg bg-primary text-black font-bold hover:bg-green-700 transition shadow-lg shadow-primary/20"
            >
                <span>SIGN IN</span>
                <span class="material-symbols-outlined text-base">arrow_forward</span>
            </button>
            
        </form>

        <p class="text-xs text-center text-neutral-500 pt-2">
            <span class="material-symbols-outlined text-sm align-middle mr-1">lock</span>
            Authorized personnel only
        </p>
    </div>

    <footer class="mt-8 text-center text-xs text-neutral-500">
        © 2026 LUXE TIME Inc. All rights reserved.
        <div class="mt-1 space-x-3">
            <a href="/privacy" class="hover:text-white transition">Privacy Policy</a>
            <span class="text-neutral-600">|</span>
            <a href="/terms" class="hover:text-white transition">Terms of Service</a>
        </div>
    </footer>

</body>
</html>